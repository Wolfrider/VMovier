<resources>

    <!-- Base application theme. -->
    <style name="AppBlackTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@android:color/black</item>
    </style>

    <style name="AppLightTheme" parent="Theme.AppCompat.Light.NoActionBar"/>

    <style name="AppFullScreenDialogTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowIsFloating">true</item>
    </style>

    <style name="AppSplashTheme" parent="AppBlackTheme">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowBackground">@drawable/splash_background</item>
    </style>

    <style name="BlackToolbar">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:src">@drawable/navigation_back</item>
        <item name="android:background">@android:color/black</item>
    </style>

    <style name="WhiteToolbar">
        <item name="android:textColor">@android:color/black</item>
        <item name="android:src">@drawable/navigation_back_black</item>
        <item name="android:background">@android:color/white</item>
    </style>

    <style name="EditText" parent="Theme.AppCompat.Light">
        <item name="colorControlActivated">@android:color/white</item>
    </style>
</resources>
